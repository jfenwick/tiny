INSTALLATION

python tiny.py projectname

The current directory will be changed to the name of projectname

NOTE: This doesn't work on Windows as there are some files in .git\objects\pack that have read only permission
