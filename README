INSTALLATION

1. Rename tiny directory to name of project

2. Delete git files:
rm -rf .git

3. Replace all instances of tiny with the name of the project:
python perlpie.py . tiny project
or
perl -p -i -e 's/tiny/project/g' `find . -type f`

4. Check to see if correct:
find . -type f -exec grep "tiny" {} \; -print
